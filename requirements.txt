# 代码质量工具
autopep8>=2.3.2
black>=25.1.0
flake8>=6.0.0
isort>=6.0.1

# 构建和发布工具
build>=0.10.0
twine>=4.0.0
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
pre-commit>=3.0.0

# Ollama API 交互依赖
requests>=2.28.2
PySocks>=1.7.1

# Web UI 依赖 - 使用已知稳定的版本组合
gradio==4.40.0
gradio-client==1.2.0
fastapi==0.112.2
pydantic==2.10.6
starlette==0.38.6
uvicorn==0.30.6

# 环境配置
python-dotenv>=0.21.0

# 实用工具(可选)
tqdm>=4.65.0
openai>=1.0.0

# AI服务相关依赖
dashscope
ruamel.yaml>=0.18.10
flask>=2.0.0

# 网络和HTTP客户端依赖
urllib3>=2.0.0
aiohttp>=3.8.0
httpx>=0.24.0
socksio>=1.0.0

# 桌面端应用依赖
pywebview>=5.4

# 异步支持
nest-asyncio>=1.6.0

# 进程管理
psutil>=5.9.0

# MCP (Model Context Protocol) 支持
aiohttp>=3.8.0  # 已包含在上面的网络依赖中

# RAG系统依赖（可选）
# 如需使用RAG功能，请安装以下依赖：
# sentence-transformers>=2.2.0
# faiss-cpu>=1.7.0
# numpy>=1.21.0
# PyPDF2>=3.0.0
# python-docx>=0.8.11
# reportlab>=4.4.2
#
# 或者使用：pip install vertex[rag]
# 或者使用：uv pip install vertex[rag]

# Web搜索工具依赖（可选）
# Web搜索工具的基础依赖（requests）已包含在主依赖中
# 如需使用完整的Web搜索功能，请安装：
# pip install vertex[web-search]
# 或者使用：uv pip install vertex[web-search]
#
# 注意：Web搜索功能需要配置API密钥：
# - 博查AI: 在配置文件中设置 web-search.bocha.sk
# - 必应搜索: 在配置文件中设置 web-search.bing.sk
